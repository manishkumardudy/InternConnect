# Backend API Endpoint
VITE_API_URL=http://localhost:5000/api

# Note: The Razorpay public key ID is securely sent by the backend create-order API response at runtime,
# so no frontend VITE_RAZORPAY_KEY_ID variable is required!
